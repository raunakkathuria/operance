# Public Repo Metadata

Status: Public export helper  
Type: Repo metadata reference  
Audience: Founder, maintainers

---

## 1. GitHub Description

Suggested repository description:

`Operance is a local-first desktop action runtime for Linux desktops, with a shared portable core, per-platform adapters, and an MCP-compatible control surface.`

Shorter fallback:

`Operance is a local-first desktop action runtime for Linux desktops, with per-platform adapters and an MCP-compatible control surface.`

---

## 2. GitHub Website

Recommended default:

- leave blank until there is a real project site

Acceptable temporary fallback:

- point to the GitHub repository itself

Do not point this field at an unstable personal machine or an internal planning document.

---

## 3. Suggested Topics

Use a small, accurate topic set:

- `linux`
- `kde`
- `wayland`
- `desktop-automation`
- `local-first`
- `mcp`
- `voice`
- `python`

Avoid adding Windows or macOS topics until those adapters actually exist in public.

---

## 4. Public Position Reminder

This repo is currently positioned as:

- Fedora KDE Wayland developer alpha
- source checkout first
- RPM base-runtime second
- tray plus click-to-talk first

Keep the metadata aligned with that support contract.
