# Changelog

## Unreleased

- Added `scripts/bundle_python_runtime.py` plus a new optional `--bundle-profile mvp` path across the packaged-asset renderer and Fedora package build helpers, so maintainers can now build an RPM that vendors the current tray UI and STT runtime dependencies from the local virtualenv instead of stopping at the base-runtime package profile.
- Hardened the bundled-runtime packaging path by stripping executable bits from vendored `site-packages` files during packaging, preventing `PySide6` helper scripts with ambiguous shebangs from breaking `rpmbuild` even though those files are runtime payload rather than installed CLI tools.
- Real-validated the new `mvp` RPM bundle profile on Fedora: the targeted packaging test slice passes, `./scripts/build_package_artifacts.sh --rpm --bundle-profile mvp --bundle-python .venv/bin/python` completes, and the finished artifact now passes `rpm -Kv`, but the profile remains experimental until the installed path is smoke-tested end to end.
- Tightened the public README to the release-tested quickstart and a small verified alpha command smoke, moving deeper setup, packaging, systemd, planner, and optional voice diagnostics back behind `docs/requirements/linux.md` instead of keeping a long command inventory in the main landing page.
- Added a platform-provider boundary under `src/operance/platforms/`, so adapter selection, Linux doctor checks, setup-step metadata, and live command verification or blocker rules now route through a host-specific provider instead of staying hardcoded across shared core modules.
- Added `docs/architecture/adapter-authoring.md` and updated the architecture, contributor, README, and Linux handoff docs to describe the current extension contract: providers own host readiness and verification rules, adapters own execution, and the portable core keeps shared tool semantics and safety.
- Moved Linux setup actions, blocked recommendations, next-step generation, and per-check remediation policy behind the `linux_kde_wayland` provider, so new OS bring-up no longer requires editing `src/operance/ui/setup.py` just to project host-specific onboarding or release-gate behavior.
- Moved Linux key-sequence translation out of `src/operance/key_presses.py` and into the Linux adapter, so shared key definitions now stay semantic and future platform backends do not need core edits just to express native input transport details.
- Added architecture guardrails to `AGENTS.md` plus new maintainer docs for public export, release sequencing, and the `v0.1.0-alpha.1` release-note draft, so the repo now carries its own open-source and release workflow instead of relying on private chat context.
- Fixed Linux app and URL launch reporting so `gtk-launch` and `xdg-open` must return success before Operance reports a launched or opened result, instead of blindly claiming success after firing a short-lived launcher helper.
- Fixed Linux URL-like launch resolution so targets like `localhost:3000` now bypass `gtk-launch` and go straight to `xdg-open` when both helpers are present, matching the intended browser-launch path on KDE desktops.
- Added an explicit `simulated` flag to transcript, transcript-file, and interactive CLI response payloads, so default developer-mode runs now declare that they stayed on mock adapters instead of looking indistinguishable from live desktop execution.
- Tightened the Linux handoff and README command examples so mock-mode testing no longer uses app-launch examples that imply a real desktop effect, and live-mode examples now call out `OPERANCE_DEVELOPER_MODE=0` plus the expected `simulated: false` payload state.
- Tightened `--supported-commands --supported-commands-available-only` so it now exposes only the commands that are both environment-ready and release-verified for the current Fedora KDE developer-alpha target, instead of treating every prerequisite-clean command as launch-ready.
- Added explicit release-verification status and target metadata to the supported-command catalog and support snapshot, so unverified commands stay visible in the full catalog without being surfaced as live in the launch-facing filtered view.
- Added a concise public repo metadata reference under `docs/release/public-repo-metadata.md`, keeping GitHub About description, website, and topic suggestions out of the main README.
- Added a Linux handoff snippet for safe mock command testing versus live Linux command testing on the current machine, and shortened the README status section into grouped alpha-facing capability bullets instead of a changelog-shaped inventory.
- Tightened the public developer-alpha wording across the README, Linux handoff doc, contributor guide, and public alpha handoff, making the Fedora KDE source-checkout path, the RPM base-runtime path, and the current tray-plus-click-to-talk stop line more explicit for a future public export.
- Expanded `.gitignore` for public-export hygiene, adding common cache, coverage, editor, and local agent-state paths so a new public repo can be initialized from a cleaner tree.
- Added `operance --version`, which prints the current Operance version plus the current git commit when checkout metadata is available, so beta testers can identify exactly what they are running without bootstrapping the daemon.
- Added build identity metadata to support snapshots and support-bundle manifests, including version plus git branch, commit, and dirty-state details when available, so issue-report artifacts can be tied back to an exact checkout instead of relying on prose.
- Added `operance.cli --version` to the default `./scripts/run_beta_smoke.sh` sequence and synced contributor or issue-report guidance around that command, so outside-developer bug reports now capture release identity by default instead of treating it as optional context.
- Added version-stamped default filenames for generated support bundles and tray-saved support snapshots, so issue-report artifacts from multiple builds stay sortable without relying on directory structure alone.
- Switched `./scripts/run_beta_smoke.sh` to print only the runnable supported-command subset while leaving the full blocked catalog in the support bundle, so the outside-developer smoke path is less noisy without losing maintainer diagnostics.
- Added a concise developer beta quickstart to `README.md` and the Linux handoff doc, so a new outside developer can reach install, version check, MVP launch, beta smoke, and support-bundle collection without reading the full project-status inventory first.
- Expanded the native package scaffolds so the built `.deb` and `.rpm` artifacts now install a runnable `/usr/bin/operance` wrapper plus the packaged Python source tree under `/usr/lib/operance`, instead of shipping only desktop-entry and user-service assets.
- Added `./scripts/run_installed_beta_smoke.sh`, which can optionally install a native package artifact, verify the installed desktop-entry and user-unit paths, and then run the safe `operance --version`, `--doctor`, runnable-command, and `--support-bundle` checks against the installed command.
- Added `./scripts/run_fedora_release_smoke.sh` plus matching setup actions for `run_fedora_release_smoke` and `run_installed_rpm_beta_smoke`, so a Fedora host can now move from RPM build to installed-package smoke through one explicit release-gate path instead of stitching package helpers together by hand.
- Expanded setup next steps so packaging-ready Fedora hosts now surface `./scripts/run_fedora_release_smoke.sh` directly, or the installed-RPM smoke command when the default RPM artifact already exists, instead of leaving the release gate buried only in the broader setup action catalog.
- Added `./scripts/run_fedora_alpha_gate.sh` plus a matching setup action, so a prepared Fedora checkout can now run `pytest`, the source-checkout beta smoke, and the package-backed release smoke through one explicit developer-alpha gate instead of following the checklist by hand.
- Fixed `build_rpm_package.sh` so it now accepts Fedora-style internal RPM filenames like `operance-0.1.0-1.fc43.noarch.rpm` and still copies the result out to the documented normalized path `operance-0.1.0-1.noarch.rpm`, which keeps the release smoke and install helpers aligned with real Fedora output.
- Tightened the Fedora release and alpha gate scripts so they now fail fast with `./scripts/install_packaging_tools.sh --rpm` when `rpmbuild` is missing, instead of spending a full test run before dying deep in the RPM build step.
- Ignored the generated `dist/` packaging tree, so running the Fedora packaging or alpha-gate helpers no longer leaves untracked artifact noise in the checkout.
- Expanded `.github/workflows/ci.yml` with a packaging-smoke job that runs the packaged-asset renderer, RPM artifact builder, installed-package smoke wrapper, and Fedora release gate in dry-run mode, so release-path scripts now get exercised on every push and pull request instead of only by hand.
- Added `docs/release/fedora-alpha-checklist.md`, which freezes the current Fedora KDE alpha stop line, source-checkout gate, RPM gate, and known packaged-path gaps in one explicit release document instead of leaving them implicit across README and Linux setup notes.
- Added `docs/release/public-developer-alpha.md` and updated the README, Linux handoff doc, and contributor guide around the same Fedora KDE developer-alpha stop line, so outside developers now have one honest handoff plus a clearer path for testing, reporting issues, or sending patches.
- Added `./scripts/install_packaging_tools.sh` plus matching setup actions for Debian or RPM packaging tools, so Fedora alpha bring-up can install `rpm-build` from the same projected setup surface instead of stopping at a missing-CLI warning.
- Expanded the existing `keys.press` path with a small allowlist of developer-oriented modifier chords like `Ctrl+C`, `Ctrl+L`, `Ctrl+R`, `Ctrl+T`, `Ctrl+W`, and `Ctrl+Shift+P`, keeping the input surface typed and validator-backed instead of turning it into open-ended keyboard automation.
- Added a structured tray last-interaction report, so successful click-to-talk runs and backend failures now project one inspectable summary plus detail list through the tray snapshot instead of relying only on tooltip text or transient notifications.
- Added a tray `Show last interaction` dialog, so developers can open that structured click-to-talk result or backend failure report directly from the product surface instead of reading raw snapshot JSON.
- Enriched click-to-talk completion notifications so successful runs now include both the final `Heard:` transcript line and the resulting response text instead of showing only the response.
- Expanded deterministic app launch and focus matching so simple app names like `open code`, `launch visual studio code`, and `switch to code` now work without going through the planner, while chained phrases like `open firefox and notify me` still stay out of the deterministic path.
- Added explicit developer URL launch support on the existing `apps.launch` path, so transcripts like `open http://localhost:3000` or `open localhost:3000` now produce URL-aware previews and result messages while Linux live execution normalizes localhost targets before `xdg-open`.
- Added voice-friendly dev-server launch phrasing on that same path, so `browse to localhost 3000` and `open url localhost port 3000` normalize into the existing URL-aware app launch flow instead of requiring literal punctuation in voice transcripts.
- Added explicit hostname browsing for `browse to` and `open url`, so developer docs and GitHub-style host or path targets now normalize to `https://...` without making generic `open <name>` ambiguous.
- Added recent-file opening on the existing `files.open` path, so `open recent file called notes.txt` now resolves against the current recent-file list and opens the matched file through the same Linux and mock file adapters instead of stopping at list-only recent-file inspection.
- Updated the supported-command catalog and tray help to render generic app patterns like `open <app name>` and `quit <app name>` instead of implying those commands are tied to the example app names in registry metadata.
- Added `operance.cli --supported-commands --supported-commands-available-only`, so beta testers can print only the currently runnable command subset instead of manually filtering blocked entries out of the full catalog.
- Routed that same runnable-command subset into setup next steps, MVP blocked or tray-already-running payloads, and `./scripts/run_mvp.sh --supported-commands --supported-commands-available-only`, so launch-time command discovery stays consistent across the main beta entrypoints.
- Added the runnable-command subset to `operance.cli --support-snapshot`, so support bundles and issue-report JSON now carry both the full command catalog and the currently runnable subset for faster maintainer triage.
- Added `files.open` across deterministic intent handling, validator and registry metadata, executor wiring, Linux `xdg-open` and mock file adapters, planner previews, supported-command catalog coverage, and desktop file transcript coverage.
- Added a baseline public-repo trust surface with GitHub Actions CI, a security policy, a code of conduct, richer package metadata classifiers, and synced contributor or Linux handoff docs for open-source beta readiness.
- Added `operance.cli --support-bundle`, which writes one redacted `tar.gz` issue-report artifact containing the aggregated support snapshot, support-summary help text, current voice-loop runtime snapshot, and any available tray or voice-loop service log excerpts.
- Added `operance.cli --support-bundle --support-bundle-out <path>`, so that same archive can be written to an explicit location while still reporting missing journal excerpts as warnings instead of failing the artifact.
- Added `./scripts/run_mvp.sh --support-bundle --support-bundle-out <path>`, so the repo-local MVP launcher now exposes the preferred bundle artifact directly instead of only the lower-level CLI entrypoint.
- Switched setup next steps from raw support-snapshot collection to `python3 -m operance.cli --support-bundle`, so onboarding points at the one-file issue-report artifact first while keeping the lower-level snapshot path available separately.
- Added a tray `Save support bundle` action, so the in-product diagnostics surface can now write the preferred redacted `tar.gz` issue-report artifact to `data_dir/support-bundles/` instead of stopping at the raw JSON snapshot.
- Updated `CONTRIBUTING.md` and the GitHub bug-report template to prefer `operance.cli --support-bundle`, keeping `--support-snapshot` as the inline fallback when maintainers need raw JSON instead of the archive.
- Added `Collect support bundle` and `Collect support snapshot` to the setup action surface, so the setup app and JSON action API can execute the preferred archive path directly instead of treating support collection as prose-only guidance.
- Added `./scripts/run_beta_smoke.sh`, so outside developers can run a fail-fast safe baseline of `--doctor`, `--setup-actions`, `--supported-commands`, and `--support-bundle` before filing bugs or changing code.
- Added `operance.cli --support-snapshot-out <path>`, so the aggregated developer support snapshot can now be persisted directly to a file while keeping the existing stdout JSON payload unchanged.
- Added `./scripts/run_mvp.sh --support-snapshot --support-snapshot-out <path>`, so the repo-local MVP launcher can collect that same redacted support snapshot directly to a file instead of requiring a raw CLI invocation.
- Added a tray `Save support snapshot` action, so the in-product diagnostics surface can now write the same redacted JSON payload to `data_dir/support-snapshots/` without requiring terminal copy-paste.
- Expanded support snapshots with a bounded tail of recent audit-log entries, so issue-report artifacts now include recent runtime history alongside doctor, setup, supported-command, and voice-loop state.
- Expanded deterministic app-command phrasing so `please open ...`, `open app ...`, `focus app ...`, and `switch to app ...` now map to the same launch or focus actions as the shorter variants instead of falling through the matcher.
- Added a shared support-snapshot surface for developer diagnostics: `operance.cli --support-snapshot` now aggregates doctor, setup, supported-command, and voice-loop state into one machine-readable payload with home-path redaction enabled by default, `--support-snapshot-raw` opts back into exact paths when needed, and the tray menu exposes that same redacted snapshot through an in-product dialog instead of requiring several separate CLI outputs.
- Extended support-snapshot collection into onboarding and MVP launch helpers: `./scripts/run_mvp.sh --support-snapshot` still prints the lower-level one-command diagnostic path for issue reporting when the raw JSON is preferable to the bundle archive.
- Removed the temporary pre-release rename compatibility layer, so runtime code, setup flows, docs, and tests now use only `operance` names and paths with no alternate service, config, or asset fallbacks.
- Added a shared supported-command help surface for first-run discovery: the tray app now exposes `Show supported commands`, the startup hint points developers at that menu, and the CLI/tray both reuse one command-catalog implementation instead of maintaining separate examples.
- Contextualized repo-local voice-loop heartbeat reporting so doctor and setup only warn about stale runtime status when an active `operance-voice-loop.service` should actually be updating it; inactive or uninstalled service states now keep the last snapshot visible without treating it as a fresh runtime failure.
- Tightened Wayland text-input readiness so doctor, setup, and the supported-command catalog now probe real `wtype` backend usability, block text injection when the compositor rejects the virtual keyboard protocol, and stop suggesting package installs when `wtype` is present but unusable.
- Added `scripts/install_wayland_input_tools.sh` plus a matching setup action, so missing `wl-clipboard` or `wtype` warnings can now point at one runnable installer helper instead of only static package guidance.
- Expanded the MVP bring-up path so setup next steps, blocked MVP launch payloads, and `./scripts/run_mvp.sh --supported-commands` now all point at the supported-command catalog instead of leaving command discovery behind a standalone CLI flag.
- Added `operance.cli --supported-commands`, which projects the typed command catalog with example transcripts plus live runtime blockers from the current doctor/setup state.
- Expanded Wayland clipboard and text-input setup remediation so that when `apt` or `dnf` is available, blocked recommendations and per-step remediation now point at the exact `wl-clipboard` or `wtype` install command instead of a generic doctor rerun.
- Added a `wayland_session_accessible` doctor and setup check, so Linux bring-up now distinguishes missing Wayland CLI tools from a process that cannot actually reach the active Wayland socket.
- Expanded setup snapshots and blocked recommendations so missing Wayland clipboard or text-input backends are now surfaced explicitly when `wl-copy`, `wl-paste`, or `wtype` are absent, instead of hiding those runtime gaps in doctor output alone.
- Added `clipboard.copy_selection` across deterministic intent handling, validator and registry metadata, executor wiring with clipboard undo, Linux `wtype` and mock text-input adapters, planner previews, CLI transcript coverage, and MCP tool schemas.
- Added `keys.press` across deterministic intent handling, validator and registry metadata, executor wiring, Linux `wtype` and mock text-input adapters, planner previews, CLI transcript coverage, and MCP tool schemas.
- Added `clipboard.paste` and `text.type` across deterministic intent handling, validator and registry metadata, executor wiring, Linux `wtype` and mock text-input adapters, planner previews, CLI transcript coverage, MCP tool schemas, corpus coverage, and doctor visibility.
- Added `windows.set_on_all_desktops` across deterministic intent handling, validator and registry metadata, executor wiring, KWin-backed Linux and mock window adapters, planner previews, CLI transcript coverage, MCP tool schemas, and corpus coverage.
- Added `windows.set_keep_below` across deterministic intent handling, validator and registry metadata, executor wiring, KWin-backed Linux and mock window adapters, planner previews, CLI transcript coverage, MCP tool schemas, and corpus coverage.
- Added `windows.set_shaded` across deterministic intent handling, validator and registry metadata, executor wiring, KWin-backed Linux and mock window adapters, planner previews, CLI transcript coverage, MCP tool schemas, and corpus coverage.
- Added `windows.set_keep_above` across deterministic intent handling, validator and registry metadata, executor wiring, KWin-backed Linux and mock window adapters, planner previews, CLI transcript coverage, MCP tool schemas, and corpus coverage.
- Added `windows.set_fullscreen` across deterministic intent handling, validator and registry metadata, executor wiring, KWin-backed Linux and mock window adapters, planner previews, CLI transcript coverage, MCP tool schemas, and corpus coverage.
- Added a bounded manual click-to-talk voice path, including the new default `operance.cli --click-to-talk` entrypoint, the lower-level `operance.cli --click-to-talk-frames N` tuning hook, tray click-to-talk control against the tray daemon session, and a reusable manual-session helper that keeps confirmations and undo in the same runtime session instead of forcing wake-word gating.
- Hardened click-to-talk backend failure handling so microphone or STT startup errors now fail closed across the tray and CLI surfaces, restore the daemon to `IDLE`, and keep the backend error visible instead of leaving stale listening state behind.
- Tightened the idle tray tooltip so it now advertises `Left-click to talk` instead of foregrounding the optional background voice loop’s benign `waiting_for_wake` state, keeping the current MVP interaction path explicit for first-run developers.
- Added a one-time tray startup notification that repeats that same `Left-click the tray icon to talk.` guidance, so `run_mvp.sh` launches directly into an explicit first action instead of relying on docs or hover discovery.
- Added `operance.cli --mvp-launch` plus repo-local `./scripts/run_mvp.sh`, so the checked-out tree now has one MVP launcher that prefers the tray when available, falls back to click-to-talk when it is not, and returns structured setup guidance when the machine is not yet ready.
- Hardened tray lifecycle for the new MVP launcher so `operance.cli --mvp-launch` now reports `already_running` when the tray user service is active, and the tray app itself rejects a second instance through a local lock file instead of spawning duplicate tray icons.
- Added repo-local `./scripts/run_click_to_talk.sh` and `./scripts/run_tray_app.sh` wrappers, so the source-checkout MVP path no longer depends on remembering the corresponding `python -m operance.cli` invocations.
- Expanded setup and Linux handoff surfaces with a dedicated click-to-talk probe action and MVP-first documentation, so the default developer bring-up path now points at manual tray or CLI invocation before wake-word tuning.
- Expanded the setup snapshot and setup-action output with explicit next-step commands, so ready machines now surface MVP entrypoints like `./scripts/run_click_to_talk.sh` and `./scripts/run_tray_app.sh` separately from remediation commands.
- Expanded `operance.cli --setup-run-recommended` so that when no setup changes remain it now returns those same next-step commands instead of ending at an empty result list.
- Added `ready_for_mvp` to the setup snapshot and tightened setup status semantics so a machine that can already run the click-to-talk MVP path now reports `summary_status: "ready"` even if packaging or optional voice extras are still incomplete.
- Expanded tray snapshots and the optional tray app with the tray daemon session’s own last heard transcript, so voice-testing developers can now distinguish STT mistakes from executor behavior without leaving the tray surface; no-transcript click-to-talk attempts now also persist in that same tray state instead of vanishing after a notification.
- Tightened tray preview semantics so background voice-loop transcript or response fields remain visible, but the tray session’s own last-command preview no longer falls back to stale background-loop replies.
- Wired primary tray activation to the same click-to-talk path as the tray menu action, so the MVP invocation path is now one click instead of requiring context-menu navigation.
- Added an OSS-readiness baseline with an MIT license, a contributor guide, a short architecture overview, and GitHub issue or pull-request templates, so external contributors now have an explicit local-core boundary and contribution workflow.
- Added explicit clipboard clear support across deterministic intent handling, registry metadata, executor undo, Linux and mock clipboard adapters, planner previews, CLI transcript coverage, MCP tool schemas, and corpus coverage.
- Added clipboard read and copy support across deterministic intent handling, validator and registry metadata, executor undo, Linux `wl-copy` or `wl-paste` adapters, mock adapters, planner previews, MCP tool schemas, doctor visibility, and corpus coverage.
- Renamed the product, Python package, CLI, scripts, service units, MCP tool and resource prefixes, environment variables, and user-facing config or data paths to `operance`, so the current runnable surface now consistently uses the new project name.
- Documented the product positioning as Linux-first with a phased cross-platform roadmap: Phase 1 Linux/KDE/Wayland, Phase 2 Windows, Phase 3 macOS, while keeping the voice, planner, safety, and MCP core portable behind per-platform adapters.
- Added `operance.cli --voice-loop-service-status` plus `operance://runtime/voice-loop-service`, so CLI and MCP clients can inspect one combined voice-loop service, config, runtime, and recommended-remediation snapshot instead of stitching together separate surfaces.
- Expanded tray voice-loop projection with the last transcript and last response from the repo-local runtime status file, so tray previews now reflect recent background-loop activity instead of only the tray session’s own daemon state.
- Added `operance.restart_voice_loop_service`, so stateful MCP sessions can trigger the shared repo-local voice-loop service restart path instead of shelling out separately when the background loop is stale.
- Added tray-side voice-loop restart control, so stale or stopped repo-local voice-loop state now surfaces a restartable control in the tray snapshot and the optional PySide6 tray app can invoke the shared restart path directly.
- Expanded the tray snapshot and optional PySide6 tray app with repo-local voice-loop runtime state, activity labels, and stale-heartbeat warnings, so tray inspection now reflects the same background-loop health that doctor and setup already project.
- Setup now treats a stale voice-loop heartbeat as a restartable service-health issue, recommending and enabling `control_systemd_user_services.sh restart --voice-loop` when the service is installed and active but its runtime heartbeat is stale.
- Added `operance://runtime/voice-loop-status`, so MCP clients can inspect the latest repo-local voice-loop heartbeat and counters without shelling out to the CLI.
- Added a repo-local voice-loop runtime status heartbeat plus `operance.cli --voice-loop-status`, and doctor/setup now expose whether that status file exists and whether its heartbeat is still fresh.
- `operance.cli --setup-run-action` and `--setup-run-recommended` now return a nonzero exit status when any executed setup action fails, so shell automation and CI no longer need to parse JSON to detect setup failures.
- Fixed `voice_loop_wakeword_customized` classification so a `voice-loop.args` file that still holds the raw fallback wake-word settings now correctly reports `warn` instead of being treated as customized just because the value came from a file.
- Clarified `voice_loop_wakeword_customized` step remediation so setup now points to config seeding when no args file exists and to the one-step apply calibration command once a voice-loop config is already present.
- Expanded setup recommendations so a present-but-default `voice-loop.args` now promotes the one-step `--apply-suggested-threshold` calibration flow plus idle false-activation evaluation instead of the older read-only calibration command.
- Added `operance.cli --apply-suggested-threshold` for wake-word calibration, so the suggested energy-detector threshold can now be written straight into the user-scoped voice-loop config without copying the shell command by hand.
- Clarified repo-local voice-loop config reporting so `--voice-loop-config` and `--use-voice-loop-config` diagnostics now distinguish between requested config and actually applied args files instead of treating a missing file as active config.
- Expanded setup recommendations so a present-but-default `voice-loop.args` now promotes wake-word calibration and idle false-activation evaluation with `--use-voice-loop-config` instead of surfacing only a passive warning.
- Expanded setup recommendations so voice-capable Linux hosts now automatically promote missing `operance-voice-loop.service` install and `voice-loop.args` seeding steps instead of leaving both as manual side actions.
- Hardened the fallback energy wake-word detector to score sustained frame energy and require a short activation streak before firing, so clipped single-frame spikes no longer count as wake hits in the Linux voice path.
- Added user-scoped install helpers for external wake-word and Kokoro model assets, so the discovered Linux voice asset paths can now be populated without manual copy commands.
- Added `operance.cli --voice-asset-paths` plus setup remediation wiring for missing voice assets, so wake-word and TTS asset warnings now point at one concrete inspection surface instead of ending at raw file-status checks.
- Added doctor and setup detection for `OPERANCE_WAKEWORD_MODEL_SOURCE`, `OPERANCE_TTS_MODEL_SOURCE`, and `OPERANCE_TTS_VOICES_SOURCE`, so setup can now execute the new voice-asset install helpers directly when external source files are already present.
- Added concrete asset guidance to voice self-test and `--wakeword-model auto` failures, so missing Kokoro or wake-word files now point back at the shared asset-path inspection command instead of ending at generic skip or parser errors.
- Expanded setup recommendations so env-driven voice asset install actions are now promoted into the recommended command list when the source files exist and the target assets are still missing.
- Expanded `operance.cli --voice-asset-paths` so it now also reports env-provided source files and ready-to-run asset install commands instead of only listing discovered target paths.
- Expanded `operance.cli --voice-asset-paths` again so each asset entry now includes the exact source env var name and an export example instead of leaving missing-source warnings implicit.
- Expanded setup output so blocked voice-asset recommendations are now surfaced explicitly when `--setup-run-recommended` has nothing runnable, instead of returning an unexplained empty result.
- Expanded setup actions so unavailable entries now carry per-action blocker text plus the next diagnostic or remediation command when one exists, instead of flattening all blocked work to `available: false`.
- Added `operance.cli --voice-loop-config` plus a matching setup inspection action, so the repo-local continuous voice loop can now report its selected args file, effective wake-word mode and threshold, loop limits, passthrough args, and any resolved `--wakeword-model auto` asset path before launch.
- Added `--use-voice-loop-config` to the wake-word diagnostics, composite voice self-test, direct bounded voice session, and direct `operance.cli --voice-loop` path, so those commands can now validate the configured background-loop threshold and wake-word mode without retyping them by hand.
- Updated the setup voice diagnostics so that once a user-scoped voice-loop args file exists, the projected probe, calibration, idle-evaluation, and self-test commands automatically reuse that config instead of testing only the raw default threshold.
- Added a doctor/setup `voice_loop_wakeword_customized` check, so the repo now surfaces whether the continuous loop is still running on raw defaults and links that warning back to `operance.cli --voice-loop-config`.
- Added `operance://runtime/voice-loop-config`, so MCP clients can inspect the effective repo-local voice-loop args file and wake-word settings without shelling out to the CLI.
- Added planner fallback cooldown tracking after repeated planner failures, exposing configured thresholds plus live failure-count and cooldown state through runtime status and `operance://runtime/planner` instead of retrying the same broken endpoint on every unmatched command.
- Added planner-runtime reset controls for long-lived tray and MCP sessions with `operance.reset_planner_runtime`, so cooldown and recent planner-error state can now be cleared without restarting the daemon after the local planner endpoint recovers.
- Expanded the optional PySide6 setup app to show projected environment checks alongside executable setup actions, so the current first-run UI no longer hides the underlying host-state gaps it is asking the user to remediate.
- Expanded the optional PySide6 setup app with dry-run action previews and per-action result details, so first-run setup debugging no longer depends on guessing what command ran or opening a separate terminal for basic output inspection.
- Added shared Kokoro TTS asset discovery plus setup and doctor visibility for those asset paths, so the setup surface can now expose a real TTS probe action when backend, playback, and model assets are all present instead of stopping at mic, wake-word, and STT tests.
- Added an energy-detector wake-word calibration probe plus a matching setup action, so the current Linux voice path can now suggest a safer threshold for noisy rooms without over-claiming model-backed calibration.
- Added a bounded idle false-activation evaluation path for the current wake-word detector plus a matching setup action, so the repo now measures the plan’s wake-word idle checkpoint instead of only exposing raw wake-word probes.
- Added `operance.cli --voice-self-test` plus setup wiring, so the current Linux voice path can now be checked through one bounded composite summary instead of stitching together separate capture, wake-word, STT, and TTS probes by hand.
- Hardened wake-word diagnostics so idle evaluation now returns threshold-tuning guidance and `operance.cli --voice-self-test` warns on false activations instead of treating any non-crashing wake-word run as healthy.
- Added shared wake-word model asset discovery plus `--wakeword-model auto`, so doctor and setup can now detect an external `operance.onnx` asset and the CLI can reuse it for model-backed probe or live-session paths without repeating the full path every time.
- Added `scripts/update_voice_loop_user_config.sh` plus calibration command hints and setup wiring, so wake-word thresholds and `--wakeword-model auto` can now be applied to the background voice-loop config without hand-editing `voice-loop.args`.
- Added recent-audit read surfaces with `operance.cli --audit-log` and `operance://runtime/audit`, so persisted runtime and planner diagnostics can now be inspected without opening SQLite directly.
- Expanded the SQLite audit log with plan-source, routing-reason, and planner-error metadata, and now persist unmatched planner-failure turns as audit entries instead of dropping them.
- Expanded tray notifications so planner fallback failures now surface as a distinct warning instead of collapsing into the generic unmatched-command notification.
- Expanded runtime status snapshots with last plan source, last routing reason, and last planner error, and added a dedicated `operance://runtime/planner` MCP resource so planner fallback state can now be inspected directly without scraping logs.
- Expanded runtime status snapshots and the live MCP runtime-status resource with the active short-lived planner context count and recent messages, so live planner fallback state can now be inspected directly.
- Expanded planner prompt generation with per-tool required-arg hints, so the local planner now sees expected argument names alongside safety metadata and example transcripts instead of inferring args only from descriptions or examples.
- Added `--planner-context-entry` support to `operance.cli --planner-prompt` and `--planner-request`, so short-lived planner context can now be injected and inspected directly from the CLI instead of only through a live daemon session.
- Added `operance.cli --planner-prompt`, so the exact system-plus-user planner messages can now be inspected directly instead of only through the full service request payload.
- Expanded planner prompt generation so the local tool catalog now carries per-tool risk tier, confirmation requirements, and example transcripts into the system prompt instead of exposing only tool names and descriptions.
- Expanded the tool registry and MCP catalog with example transcripts drawn from the repo’s implemented command surface, so clients can inspect fixture-like invocation examples alongside schemas and safety metadata.
- Expanded the tool registry and MCP catalog with explicit undo summaries and non-undo reasons, centralizing the rollback guidance used by both tool inspection and pending confirmation metadata.
- Expanded the tool registry and MCP catalog with per-tool result schemas and allowed side-effect metadata, so clients can inspect approved outcomes and impact alongside input schemas and risk tiers.
- Added planner readiness checks to doctor and setup, so the repo now reports whether planner fallback is enabled, whether the configured local endpoint is healthy, and exposes a dedicated setup action for `operance.cli --planner-health`.
- Expanded pending confirmation metadata with plan source and risk tier, and updated the tray confirmation dialog to surface that context before execution.
- Updated the tray confirmation dialog to include an explicit Cancel action inside the dialog itself, so the current UI now matches the plan’s confirmation UX requirement instead of forcing cancellation back through the tray menu.
- Added configurable confirmation timeouts across daemon, status, tray, and MCP flows, so pending commands now expose timeout behavior in their metadata and late confirmation replies expire closed instead of executing stale plans.
- Expanded the structured setup surface with executable voice diagnostics, so it now exposes audio-device listing plus bounded microphone, wake-word, and STT probe actions whenever the current checkout can actually run those CLI paths.
- Added bounded planner transport retries plus `operance.cli --planner-health`, so the local planner client can retry transient transport failures and probe sibling `/v1/models` or `/health` endpoints before live fallback is relied on.
- Expanded confirmation-gated destructive actions across more domains with `apps.quit`, `network.disconnect_current`, and `files.delete_file`, wiring them through deterministic matching, validator and policy enforcement, Linux and mock adapters, planner previews, daemon and session flows, CLI output, and MCP schemas.
- Expanded the structured setup surface with package lifecycle actions: it now projects package scaffold rendering, per-format package builds, and native package install or uninstall actions when the required tooling and default artifact paths are present.
- Normalized RPM package-build output so `build_rpm_package.sh` and `build_package_artifacts.sh` now copy the built `.rpm` back into the documented root artifact directory, making the install-helper path contract real instead of relying on the internal rpmbuild tree.
- Expanded the structured setup surface so installed-but-disabled or installed-but-inactive tray and voice-loop user services now expose `enable_*` and `restart_*` setup actions, and step remediation now points at `control_systemd_user_services.sh` instead of reinstall commands for those states.
- Expanded doctor and setup projection so `voice_loop_user_config_available` now reports the active user or system voice-loop args path, and the setup action surface exposes `install_voice_loop_user_config` for seeding `~/.config/operance/voice-loop.args` on demand.
- Added `scripts/install_voice_loop_user_config.sh`, a dry-run-safe helper that seeds `~/.config/operance/voice-loop.args` from the packaged or repo example file and refuses to overwrite an existing config unless forced.
- Updated the packaged voice-loop launcher so it now prefers `~/.config/operance/voice-loop.args` before `/etc/operance/voice-loop.args`, keeping per-user service configuration out of root-owned paths by default.
- Expanded `scripts/control_systemd_user_services.sh` to support `enable --now` and `disable --now`, so the same helper can now activate packaged or repo-local user units instead of only starting and stopping them.
- Expanded doctor service discovery so `operance-tray.service` and `operance-voice-loop.service` now count as present when they come from either repo-local or packaged user-unit locations.
- Added a packaged `/etc/operance/voice-loop.args.example` sample so the packaged voice-loop launcher now has a documented starting point for optional loop flags.
- Added a packaged `voice-loop-launcher` asset so the packaged `operance-voice-loop.service` scaffold can read optional args from `/etc/operance/voice-loop.args` instead of hardcoding all loop flags in the unit file.
- Expanded the packaged asset renderer plus the Debian and RPM package scaffolds so they now carry both `operance-tray.service` and `operance-voice-loop.service`.
- Added optional repo-local and user-scoped args-file discovery to `scripts/run_voice_loop.sh`, and updated the repo-local voice-loop systemd unit to rely on that launcher search order instead of hardwiring one args file path.
- Added `scripts/control_systemd_user_services.sh`, a shared repo-local helper for starting, stopping, restarting, or checking the tray and voice-loop user services from the checked-out tree.
- Expanded the structured setup surface with a dedicated `install_voice_loop_service` action and remediation hints for the repo-local `operance-voice-loop.service` unit when the current machine is ready to run it.
- Expanded `scripts/tail_systemd_user_service_logs.sh` so it can now inspect the repo-local `operance-voice-loop.service` journal as well as the tray service.
- Expanded the local install/uninstall orchestrators and doctor output to manage and report the repo-local `operance-voice-loop.service` user unit alongside the existing tray service.
- Added repo-local systemd user-service install and removal helpers for the continuous voice loop, backed by a dedicated `operance-voice-loop.service` template that runs `scripts/run_voice_loop.sh`.
- Added `scripts/run_voice_loop.sh`, a repo-local launcher for the continuous voice loop that forwards extra `operance.cli` arguments after `--`.
- Added a continuous live voice loop path with `operance.cli --voice-loop`, including optional command or frame stop criteria for regression and an interrupt-safe summary when the capture loop is stopped.
- Added bounded live voice-session playback for synthesized response audio, so the saved-response TTS path can now also play replies through the Linux playback sink during the current bounded voice loop.
- Added a Linux audio playback sink plus `operance.cli --tts-play`, so saved Kokoro probe output can now be played through `pw-play`, `paplay`, or `aplay`, with doctor and setup visibility for playback tooling.
- Added optional response-audio synthesis for bounded live voice sessions, so the existing wake-word/STT daemon loop can now save Kokoro-backed WAV responses when given model assets and an output directory.
- Added an optional Kokoro-backed TTS probe path, including speech-synthesis contracts, CLI output, doctor and setup visibility for the backend, and `voice` extra support for installing `kokoro-onnx` plus `soundfile`.
- Expanded the doctor output and setup snapshot to report whether `apt` and `dnf` are available for the current native-package install and uninstall helpers.
- Added `scripts/uninstall_native_package.sh`, a dry-run-safe native-package uninstall helper for removing an installed Operance package through `apt` or `dnf`.
- Added `scripts/install_package_artifact.sh`, a dry-run-safe native-package install helper for built `.deb` and `.rpm` artifacts with explicit `apt` or `dnf` selection.
- Added `operance.cli --setup-app`, an optional PySide6 setup window that projects the structured setup snapshot and runs selected or recommended setup actions through the shared setup runner.
- Tightened the setup recommendation planner so overlapping setup steps collapse into one minimal repo-local command set when a broader setup action already subsumes narrower ones.
- Added setup-action execution helpers plus `operance.cli --setup-run-action` and `operance.cli --setup-run-recommended`, giving the new setup surface a dry-run-safe runner instead of inspection only.
- Added structured setup actions to the setup snapshot and exposed them through `operance.cli --setup-actions`, giving future first-run UI work a stable action surface.
- Added `src/operance/ui/setup.py` and `operance.cli --setup-snapshot`, giving the repo a structured setup-status projection over the existing doctor checks for future first-run UI work.
- Added `scripts/build_package_artifacts.sh`, a one-command package build wrapper that reuses the existing Debian and RPM helper scripts and supports per-format selection.
- Expanded the doctor output to report whether `dpkg-deb`, `rpmbuild`, and `tar` are available for the current package scaffold and artifact build path.
- Added `scripts/build_package_scaffolds.sh`, a one-command wrapper that renders both the Debian and RPM staging trees from the shared packaged asset templates.
- Added `packaging/rpm/operance.spec.in` and `scripts/build_rpm_package.sh`, giving the repo an RPM package staging helper for the current tray assets and shared packaged metadata.
- Added `packaging/deb/control.in` and `scripts/build_deb_package.sh`, giving the repo a Debian package staging helper for the current tray assets and shared packaged metadata.
- Added shared packaged desktop-entry and systemd-unit templates plus `scripts/render_packaged_assets.sh`, giving later `deb` and `rpm` scaffolds a reusable asset-rendering base.
- Added `scripts/tail_systemd_user_service_logs.sh`, a repo-local journal helper for reading recent logs from the tray user service.
- Expanded the doctor output to report whether the repo-local tray service is enabled and active in the current user session, not just whether the unit file exists.
- Added `scripts/uninstall_local_linux_app.sh`, a matching source-checkout uninstall orchestrator that removes the tray service setup and can optionally delete the local virtual environment.
- Added `scripts/install_local_linux_app.sh`, a one-command source-checkout installer that composes the repo-local bootstrap and tray service setup steps.
- Added `scripts/uninstall_systemd_user_service.sh` and doctor visibility for `systemctl` plus installed tray service state, giving the repo-local user-service path a matching rollback and inspection surface.
- Added `packaging/systemd/operance-tray.service.in` and `scripts/install_systemd_user_service.sh`, giving the repo a source-checkout systemd user-service scaffold for the current tray process with dry-run and render-only coverage in tests.
- Added `scripts/run_demo.sh` plus checked-in replay and confirmation fixtures under `demo/`, giving the repo a repeatable deterministic local demo path on mock adapters.
- Added `scripts/install_linux_dev.sh`, a reproducible repo-local Linux bootstrap script that creates a virtual environment, installs the editable package with optional `ui` and `voice` extras, and runs the doctor check, with dry-run coverage in the test suite.
- Expanded the minimal tray surface with structured confirmation-dialog metadata, deduplicated tray-side failure notifications, and a PySide6 confirmation prompt before executing pending gated actions.
- Added a first tray-status surface, including a tray-oriented status projection, a minimal optional PySide6 tray app with confirm/cancel/undo actions, CLI tray inspection and launch commands, doctor visibility for the UI backend, and an optional `ui` dependency extra.
- Added an optional custom-model openWakeWord wake-word backend, including explicit model-path wiring in the CLI and bounded live voice session, doctor visibility for backend availability, and `voice` extra support for installing `openwakeword`.
- Added a bounded live voice-session path that chains captured Linux frames through wake-word detection, optional Moonshine-backed STT, and daemon transcript handling, including confirmation follow-ups within the same voice session and a new CLI session command.
- Added an optional Moonshine-backed STT probe path, including a speech-transcriber builder, PCM-to-float frame conversion, a frame-driven STT probe runner, CLI probe output, doctor visibility for backend availability, and an optional `voice` dependency extra.
- Added byte-backed `AudioFrame` support plus a frame-driven wake-word probe path, including Linux PCM capture chunks, a threshold detector, a wake-word probe runner, and new CLI probe commands for local voice-pipeline plumbing checks.
- Added Linux microphone discovery and frame capture probes through `audio.capture`, including `pactl` input discovery, `pw-record` or `parecord` frame capture, new CLI probe commands, and doctor visibility for Linux capture tooling.
- Added confirmation-gated `network.connect_known_ssid` support across deterministic intent handling, validator/policy enforcement, executor wiring, Linux and mock adapters, planner previews, daemon/session flows, CLI output, and MCP schemas, completing the initial Tier 0 to Tier 2 runtime tool set from the plan.
- Added confirmation-gated, undoable `files.rename` and `files.move` support for desktop entries across deterministic intent handling, validator/policy enforcement, executor wiring, Linux and mock adapters, planner previews, daemon/session flows, CLI output, and MCP schemas.
- Desktop-scoped file actions now reject path-like entry names before execution, closing a missing safety check in the earlier desktop file slice.
- Added affected-resource and rollback-hint metadata to pending confirmation snapshots, MCP confirmation responses, and the live confirmation resource for future UI and client flows.
- Added expected-result assertions to the stateful MCP fixture runner so JSONL MCP fixtures now report pass/fail counts instead of only replaying calls.
- Added session-scoped undo inspection and MCP undo control, including `operance.undo_last_action`, `operance://runtime/undo`, and undo availability in runtime status snapshots.
- Expanded pending confirmation inspection to include the original transcript and normalized pending action metadata in runtime status snapshots and MCP confirmation payloads.
- Added a stateful CLI MCP fixture runner so local JSONL scripts can exercise confirmation-gated MCP flows through one shared server session.
- Added live MCP runtime resources for `operance://runtime/status` and `operance://runtime/confirmation`, exposing session-scoped daemon and pending-confirmation state.
- Added human-readable pending plan previews to runtime status snapshots and MCP confirmation-gated responses, reusing the shared plan preview renderer.
- Added MCP-side confirmation resolution with `operance.confirm_pending` and `operance.cancel_pending`, so a stateful MCP session can complete or cancel gated commands instead of only receiving `awaiting_confirmation`.
- Expanded confirmation-gated destructive actions into more domains: disabling Wi-Fi and setting very high volume now require confirmation across deterministic, planner, and MCP-validated runtime paths.
- Added pending confirmation execution so risky commands can be confirmed or canceled in the same daemon session, with status snapshots exposing the pending plan state.
- Added confirmation-gated `files.delete_folder` support for desktop folders through the validated runtime path.
- Added confirmation-gated `windows.close` support with KWin-backed execution underneath the validated runtime path.
- Added KWin-backed `windows.restore` support on Linux.
- Added `audio.mute_status` across deterministic intent handling, executor wiring, CLI output, corpus coverage, and planner preview support.
- Added KWin-backed `windows.maximize` support on Linux.
- Added `network.wifi_status` across deterministic intent handling, executor wiring, CLI output, corpus coverage, and planner preview support.
- Added opt-in planner fallback in the daemon for high-confidence unmatched final transcripts through the local planner client, parser, validator, policy, and executor path.
- Added KWin-backed `windows.minimize` support on Linux.
- Added KWin `WindowsRunner` support for `windows.list` and `windows.switch` on Linux.
- Added `audio.get_volume` command support across deterministic intent handling, executor wiring, CLI, and MCP schemas.
- Added KWin-backed `apps.focus` on Linux with bounded D-Bus timeouts and launch fallback.
- Prefer Linux desktop services for notifications and battery status through freedesktop notifications and UPower.
- Added command-backed Linux desktop adapters for launch, focus, notifications, battery, audio, network, and desktop file actions.
- Added explicit `KISS`, `YAGNI`, and `DRY` workflow rules to the repo guidance.

## 0.1.0

- Initial Phase 0A scaffold and portable core slices through planner contracts.
